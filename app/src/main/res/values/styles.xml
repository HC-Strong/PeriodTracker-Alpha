<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PeriodAddDialogButtons" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:textSize">@dimen/small_text_size</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PosButton</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/NegButton</item>
    </style>

    <style name="PosButton" parent="PeriodAddDialogButtons">
        <item name="android:textColor">@color/bloodRed</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <style name="NegButton" parent="PeriodAddDialogButtons">
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <style name="CalendarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:dateTextAppearance">@style/CalendarText</item>
    </style>

    <style name="CalendarText" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
